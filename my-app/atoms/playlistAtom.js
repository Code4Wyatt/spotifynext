import { atom } from "recoil";

export const playlistIdState = atom({ key: "playlistIdState", default: '10K6eG7HZNnNOGglZcNttP'});

export const playlistState = atom({ key: "playlistState", default: null });